# AMD GPU Support
# ROCm userspace is NOT needed — Ollama bundles its own ROCm runtime.
# The kernel-level AMD driver comes from firmware-amd-graphics (in neuraldrive.list.chroot).
# rocm-smi-lib could be added back later for GPU monitoring if needed.
