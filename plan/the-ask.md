I want to build a LiveCD / LiveUSB linux distro for running LLMs as a headless server. The final product should boot from either CD or USB. The server should be able to load LLMs from the bootable media, as well as download additional models when booted from USB (they are stored to the boot media so that they are available for use on next boot). The system should be able to run multiple models concurrently. The system should support GPU acceleration and have good driver support for modern and popular video cards. The system should be lightweight and optimized for performance, with a minimal set of pre-installed software to reduce resource usage. The user interface should be simple and accessible, allowing users to easily manage and run their LLMs without needing extensive technical knowledge. It should have a lightweight text interface for managing the models and monitoring their performance. It should also have a web-based interface for remote management and monitoring. The system should be designed with security in mind, ensuring that the models and data are protected from unauthorized access. If the system is booted from USB, it should have the capability to persist user data and model configurations across reboots. 

Additionally, the system should run minimal services (only essential ones, SSH is optional) and have robust security features to protect against unauthorized access and ensure the integrity of the models being used.

The final result should include a toolkit for users to easily create their own custom LiveCD/LiveUSB images with their preferred LLMs and configurations. This toolkit should be user-friendly and well-documented, allowing users to customize their images without needing extensive technical knowledge.

The LLMs should be accessible locally on the machine, and the system should provide a simple interface for users to interact with the models, such as a command-line interface and via the web-based dashboard. The system should also support remote access, allowing users to manage and interact with their LLMs from other devices on the same network, making it accessible to coding agents, etc.

If the system requires scratch space for downloading models or storing user data, it should be designed to use the available storage on the boot media efficiently, and provide clear instructions for users on how to manage their storage. If the boot media is non-persistent (like a CD), the system should provide options for users to connect external storage devices for additional space, or use a RAM disk for temporary storage.

Build a detailed plan for how to create this LiveCD / LiveUSB linux distro. Include specifics, like which base distribution to use, how to set up the environment for LLMs, how to handle GPU support, and how to create the toolkit for users to customize their images, the security measures to implement, and how to ensure persistence of data when booted from USB, and how to create the web-based interface for remote management and monitoring and the text interface for managing the models and monitoring their performance locally. The plan should be comprehensive and cover all aspects of the project, from the initial setup to the final deployment. Think through each step carefully and provide detailed instructions for each part of the process, find areas that may have been missed and address them in the plan. The plan should be multiple files, each covering a specific aspect of the project, and should be organized in a way that makes it easy for users to follow and understand, and should be organized sequentially so that implementing the plan is straight forward.

When the plan is written, review the plan carefully to ensure it is complete and covers all necessary aspects of the project. Review it multiple times from multiple perspectives to ensure that it is comprehensive and addresses all potential issues that may arise during the implementation of the project. Make sure that the instructions are clear and easy to follow, and that the plan is organized in a logical manner. Consider potential challenges and provide solutions for them in the plan. Finally, ensure that the plan is well-documented and includes references to any resources or tools that may be needed for the implementation of the project.
